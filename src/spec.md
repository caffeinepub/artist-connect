# Specification

## Summary
**Goal:** Grant admin privileges to the current authenticated user.

**Planned changes:**
- Add the current user's principal to the admin list in the backend
- Ensure admin privileges persist across canister upgrades
- Make Stripe settings visible in the account dashboard for admin users

**User-visible outcome:** The user will have admin access and can view payment configuration (Stripe settings) in their account dashboard.
